import styles from './index.module.scss';

interface Props {
    // username: string;
}

const Menu: React.FC<Props> = (props) => {
    return (
        <div className={styles.container}>
            
        </div>
    );
};

export default Menu;
